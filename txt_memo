-- html
lisible par balise <audio> : mp3, ogg, m4a, flac, mpeg, wav, m4p, aac
illisible : wma, asf
à tester : 3gp

// get a project
git clone https://github.com/_pseudo_/_projectname_

// get news
git pull

// send
git push

// manag github repo
need token

(after cloning repo)
$ cd _folder_
$ git add .
$ git commit -m "_message_"
$ git push
maybe error because in www folder, so : $ git config --global --add safe.directory _folder_abs_path_
after error : $ git push

// create token
create token by password, create name, check permissions (just add "commit"), done.
infos : https://docs.github.com/fr/authentication/keeping-your-account-and-data-secure/managing-your-personal-access-tokens
